## Packages
framer-motion | Smooth page transitions and layout animations
date-fns | Date formatting for dashboards and lists
lucide-react | Icons for the UI (already in base, but essential)
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
- Using Replit Auth for authentication (/api/login, /api/logout)
- API routes follow REST patterns defined in shared/routes.ts
- Brief generation is an async process triggering AI
